import styled from 'styled-components';

const StyledHeader = styled.header`
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 24px;
  height: 50px;
  box-shadow: 1px 2px 3px var(--main-shadow-color);
  z-index: 1;
  position: relative;
`;

export default StyledHeader;
